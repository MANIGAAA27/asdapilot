<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Canonical template for a __c Custom Object. Copy this verbatim and change ONLY:
         * <label> / <pluralLabel>            display strings
         * <description>                       what the object stores
         * <nameField><label>                  the Name column's display label
         * <sharingModel>                      ReadWrite | Private | Read
         (For MasterDetail children: omit <sharingModel> entirely — see Object Metadata Rule 1)
         (Do NOT add <apiVersion>: CustomObject XML rejects it — see SFDX Rule 7)
    -->
    <label>My Object</label>
    <pluralLabel>My Objects</pluralLabel>
    <description>Short description of what this object stores.</description>
    <nameField>
        <label>Name</label>
        <type>Text</type>
    </nameField>
    <deploymentStatus>Deployed</deploymentStatus>
    <sharingModel>ReadWrite</sharingModel>
    <enableActivities>true</enableActivities>
    <enableReports>true</enableReports>
    <enableSearch>true</enableSearch>
</CustomObject>